[phases.setup]
nixPkgs = ['nodejs', 'npm']

[phases.build]
cmds = ['cd server && npm install && npx prisma generate && npx prisma migrate deploy && cd ../client && npm install && npm run build']

[start]
cmd = 'cd server && node seed.js && node index.js'